ДЗ - 3

- начать использовать данные с сервера
- На главной странице грузим 10 ракеток из общего списка и подборку топ-10. Отображаем 2 подборки
  - что за топ 10?
  - Как именно "грузим" - карусель? как расположены эти подборки?
- На странице rackets грузим 20 ракеток и отображаем их
- Сделать отдельную страницу для топ-10
- добавить индикаторы загрузки (Suspense/loading.tsx)
- на странице ракетки грузить данные о ракетке по id. Необходимо удалить generateStaticParams
- сделать отдельные 404 страницы для страницы ракетки и общую

---

Анализ репозитория препода https://github.com/zubkov7/nextjs-20260602

- Что такое container для каждого компонента, зачем они нужны?
- В чём разница между использованием "export const Racket: FC<Props> = ({ racket }) => в объявлении функции и вариантом "export const RacketCard = ({ id, name, imageUrl, showName = true }: Props) =>"

Уточняющие вопросы:

1. Объясни зачем нужен интерфейс Racket и зачем нужен тип ServiceResponse. Чтобы мы знали какие пропсы можно передавать, а каких не существует?
2. Почему Racket объявлен интерфейсом, а ServiceResponse объявлен типом? В чем разница?
3. Зачем нужен параметр limit в getRackets? Т.е. вызов getRackets будет делаться на каждое нажатие "след. страница", правильно?
4. Откуда взялась вот эта адресная строчка с квери параметрами: "${BASE_API_URL}/products?page=${page}&limit=${limit}"? - это api из бэка
5. Не понял куда мне нужно добавить { cache: 'no-store' }? И что дает этот параметр? - выключает кеширование fetch
6. Каким образом возврат { isError: false, data: undefined } вызовет 404 страницу, как это работает? - контейнеры читают isError и рендерят div с ошибкой, но вряд ли это правильный подход
7. Что означают фигурные скобки в строчке const { product } = await res.json();? - это деструктуризация объекта, вытаскивающая свойство product
8. Что значит комментарий "покажет ближайший not-found.tsx"? - ближайший по файловому роутингу
9. Что делает компонент Suspens? - обёртка, которая будет рендерить заглушку, пока грузятся данные; используется вместо loading.tsx если нужно обернуть только часть контента на странице.

---

ДЗ-4

- добавить метадату для страниц. Для страницы ракетки должна формироваться динамически
- добавить обработку ошибок (error.tsx и global-error.tsx)
- добавить ревалидацию топ-10 сегмента через revalidateTag
- добавить индикатор клиентской навигации
- прочитать документацию: https://nextjs.org/docs/app/guides/caching

Вопросы:

- Почему в одних местах нужен default export, а в других именной export
- В чем разница между import type { Metadata } from 'next'; и import { Metadata } from 'next';

- Проверь title и description на каждой странице. Я не уверен что написал туда нужный текст, т.к. у меня нет четкого понимания что должно быть в заголовке, а что в описании. Предложи мне правки
- Что такое unstable-retry в компонентах ошибок?
- Что такое ревалидация (revalidate) кеша? Как она пишеться в next.js
- Как сделать красивый индикатор клиентской навигации?
